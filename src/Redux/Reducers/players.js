const INITIAL_STATE = {
  players: {
    name: '',
    email: '',
  },
};

function players(state = INITIAL_STATE, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default players;
